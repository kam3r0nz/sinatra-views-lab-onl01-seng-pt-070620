<!DOCTYPE html>
<html>
    <head></head>
    <body>

        <h1>Today</h1>

        <% require 'date' %>
        <% d = DateTime.now %>
        <p>The date is <%= d.strftime("%A, %B %d, %Y") %></p>

    </body>
</html>